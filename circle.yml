# The deployment section should be excluded until
# it is explicitly included in module 5

#deployment:
#  dockerhub:
#    branch: master
#    commands:
#      - $curl -H"Content-Type:application/json" --data'{"build":true}' -X POST <https://registry.hub.docker.com/u/fred2420/test3/trigger/95cfff65-7918-4cec-9b5f-ba74f8eccb5f/>

test:
  override:    
    - mocha
